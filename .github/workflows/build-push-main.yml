name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    permissions:
      packages: write
      contents: write
    uses: beep-industries/actions/.github/workflows/build-push-main.yml@0.0.5
    secrets: inherit
