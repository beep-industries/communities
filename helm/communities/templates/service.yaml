apiVersion: v1
kind: Service
metadata:
  name: {{ include "communities.fullname" . }}
  labels:
    {{- include "communities.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.apiPort }}
      targetPort: api
      protocol: TCP
      name: api
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "communities.selectorLabels" . | nindent 4 }}
