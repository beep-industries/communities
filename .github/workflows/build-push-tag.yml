name: Release

on:
  push:
    tags:
      - "*.*.*"

permissions:
  packages: write
  contents: write

jobs:
  build-and-push:
    uses: beep-industries/actions/.github/workflows/build-push-tag.yml@0.0.5
    secrets: inherit
